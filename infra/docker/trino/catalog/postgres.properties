# Trino catalog: operational FHIR database (read-side analytics only).
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/fhir
connection-user=medflow
connection-password=medflow_dev_password
