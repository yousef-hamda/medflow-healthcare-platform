# dbt_utils provides accepted_range / surrogate-key helpers used in schema.yml.
# Install with `dbt deps` before `dbt build`.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
